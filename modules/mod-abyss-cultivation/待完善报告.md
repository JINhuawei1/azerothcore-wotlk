# mod-abyss-cultivation 待完善报告

## 1. 当前结论

按你的要求，以下功能已经进入移除状态：

- 图鉴功能
- 局内构筑功能
- `_深渊局内效果模板`
- `_深渊事件模板`
- `_深渊品质配置`

当前工作重点已经从“扩玩法页签”切换为：

- 清理这些功能的剩余代码
- 确保模块只保留你要的主链
- 防止残余请求、残余 SQL、残余调试命令继续引用这些已删除功能

## 2. 已完成的移除工作

### 2.1 客户端

- 已移除 `图鉴` 页签
- 已移除 `局内构筑` 页签
- 已移除对应请求与解析入口

### 2.2 服务端通信

- 已移除 `REQ_CODEX`
- 已移除 `REQ_DRAFT`
- 已移除 `REQ_BLESSINGS`
- 已移除 `REQ_CURSES`
- 已移除 `REQ_EVENT`
- 已移除 `PICK_BLESSING`
- 已移除 `PICK_CURSE`
- 已移除 `PICK_EVENT`
- 已从 `REQ_ALL` 返回内容中去掉图鉴和局内构筑数据

### 2.3 SQL

- 已从角色库 SQL 中移除 `_玩家深渊图鉴`
- 已从世界库 SQL 中移除：
  - `_深渊局内效果模板`
  - `_深渊事件模板`
  - `_深渊品质配置`

## 3. 仍需继续清理的残留

当前主要工作已从“删功能”推进到“删字段和映射”阶段，字段级与缓存级残留已基本清空。

已完成的代码层深清：

- `codex` 整条内部数据链已删除
- `draft` 主体结构与缓存已删除
- 运行时 bonus 已不再依赖 draft
- 开局和击杀流程已不再自动准备 blessing/curse/event
- 掉落品质选择已不再依赖 `_深渊品质配置` 权重表
- `effect/event` 的死辅助函数已删除
- 章节/玩家/局内状态中的旧字段映射已同步清理

## 4. 当前保留功能

当前模块对外只保留：

- 专属装备
- 装备图鉴
- 剧情章节
- 奖励历史

## 5. 下一步建议顺序

### 第一优先级

- 继续删除服务端残余方法与结构体
- 清理残余调试命令和统计输出

### 第二优先级

- 检查 `BeginPlayerRun / EndPlayerRun / Reward` 是否还依赖已删除功能
- 把掉落逻辑改成不依赖品质配置表

### 第三优先级

- 再做一次全局引用扫描
- 确认没有残留请求、残留 SQL、残留日志

## 6. 当前风险

当前最大风险不是 UI，而是：

- 服务端内部仍残留一部分已删除功能的代码
- 如果后续继续删不彻底，可能留下编译错误或死分支

所以下一步不应该继续加功能，而应该优先做“残余代码彻底清理”。
